import { hosts } from '../Datas/HostsList'

function Hosts() {
    const hostsList = hosts;

    return (
        <div>
            <p>test</p>
        {console.log(hostsList)}
       </div>
    
    );
}

export default Hosts